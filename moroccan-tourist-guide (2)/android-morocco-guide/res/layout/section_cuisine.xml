<?xml version="1.0" encoding="utf-8"?>
<LinearLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="@dimen/spacing_xl">

    <!-- Section Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="@dimen/spacing_md"
        android:layout_marginBottom="@dimen/spacing_md">

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_cuisine"
            app:tint="@color/terracotta" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="@dimen/spacing_sm"
            android:text="@string/cuisine_title"
            style="@style/TextAppearance.Maghrib.Headline3" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/action_view_all"
            android:textColor="@color/terracotta"
            android:textSize="14sp" />

    </LinearLayout>

    <!-- Cuisine Cards Horizontal Scroll -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="none"
        android:clipToPadding="false"
        android:paddingHorizontal="@dimen/spacing_md">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Tagine -->
            <include
                layout="@layout/item_cuisine"
                android:id="@+id/cuisine_tagine" />

            <!-- Couscous -->
            <include
                layout="@layout/item_cuisine"
                android:id="@+id/cuisine_couscous" />

            <!-- Pastilla -->
            <include
                layout="@layout/item_cuisine"
                android:id="@+id/cuisine_pastilla" />

            <!-- Harira -->
            <include
                layout="@layout/item_cuisine"
                android:id="@+id/cuisine_harira" />

        </LinearLayout>

    </HorizontalScrollView>

</LinearLayout>
